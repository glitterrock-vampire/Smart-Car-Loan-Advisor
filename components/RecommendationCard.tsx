// This component is no longer used directly. 
// Its functionality has been largely moved to CompactLoanCard.tsx 
// and structured within RecommendationsDisplay.tsx.
// This file can be deleted or kept as a reference for individual detailed views if needed later.

import React from 'react'; // Keep React import for safety if file is not immediately deleted

const RecommendationCard_DEPRECATED: React.FC = () => {
  return null; 
};

export default RecommendationCard_DEPRECATED;
